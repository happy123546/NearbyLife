<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmdp.mapper.OperationLogMapper">

    <insert id="saveOperationLog">
        insert into operation_log(
            user_id,
            operation_desc,
            method_name,
            cost_time,
            status,
            error_msg,
            create_time
        ) values(
            #{userId},
            #{operationDesc},
            #{methodName},
            #{costTime},
            #{status},
            #{errorMsg},
            #{createTime})
    </insert>
</mapper>